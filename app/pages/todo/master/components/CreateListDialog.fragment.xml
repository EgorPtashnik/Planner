<core:FragmentDefinition
    xmlns='sap.m'
    xmlns:form='sap.ui.layout.form'
    xmlns:core='sap.ui.core'>

    <Dialog
        title='Создать Список'
        stretch='{device>/system/phone}'>

        <content>
            <form:SimpleForm
                layout='ColumnLayout'
                editable='true'>
                <form:content>
                    <Label text='Имя' required='true' />
                    <Input
                        fieldGroupIds="CreateTodoListFG"
                        width='100%'
                        value='{state>/CreateListDialog/name}' />

                    <Label text='Приоритет' required='true' />
                    <Select
                        fieldGroupIds="CreateTodoListFG"
                        selectedKey='{state>/CreateListDialog/priority}'
                        items='{common>/PriorityCode}'>
                        <items>
                            <core:Item key='{common>code}' text='{common>name}' />
                        </items>
                    </Select>

                    <Label text='Тег' />
                    <ComboBox
                        id='idCreateListDialogTagComboBox'
                        selectedKey='{state>/CreateListDialog/tag_ID}'
                        items='{todo>/ListTag}'>
                        <items>
                            <core:Item key='{todo>ID}' text='{todo>name}' />
                        </items>
                    </ComboBox>

                    <Label text='Описание' />
                    <TextArea
                        rows='5'
                        value='{state>/CreateListDialog/info}' />
                </form:content>
            </form:SimpleForm>
        </content>
    
        <buttons>
            <Button
                press='onCreateList'
                type='Emphasized'
                text='Создать'
                tooltip='Создать' />

            <Button
                press='onPressCloseDialog'
                type='Transparent'
                text='Отменить'
                tooltip='Отменить' />

        </buttons>

    </Dialog>

</core:FragmentDefinition>