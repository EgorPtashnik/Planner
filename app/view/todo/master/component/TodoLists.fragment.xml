<core:FragmentDefinition
    xmlns='sap.m'
    xmlns:core='sap.ui.core'>

    <Table
        id='idTodoListsList'
        inset='true'
        items='{
            path: "todo>/TodoList",
            parameters: { $count: true }
        }'>

        <noData>
            <IllustratedMessage
                title='Списки еще не добавлены'
                description='Создайте новый список чтобы увидеть данные.'
                illustrationType='sapIllus-NoActivities' />
        </noData>

        <headerToolbar>
            <OverflowToolbar design='Solid'>
                <Title
                    text='Списки ({config>/todoListCount})'
                    visible='{= !%{config>/showTodoListsSearch} }' />

                <VBox
                    width='100%'
                    visible='{config>/showTodoListsSearch}'>
                    <SearchField
                        change='onChangeTodoListsSearch'
                        showSearchButton='false'>
                        <layoutData>
                            <OverflowToolbarLayoutData priority='NeverOverflow' />
                        </layoutData>
                    </SearchField>
                </VBox>

                <ToolbarSpacer />

                <ToggleButton
                    icon='sap-icon://search'
                    tooltip='Поиск'
                    pressed='{config>/showTodoListsSearch}'>
                    <layoutData>
                        <OverflowToolbarLayoutData priority='NeverOverflow' />
                    </layoutData>
                </ToggleButton>
            </OverflowToolbar>
        </headerToolbar>

        <columns>
            <Column>
                <Text text='Список' />
            </Column>
        </columns>

        <items>
            <ColumnListItem
                press='onPressTodoListItem'
                navigated='{= %{/detailID} === %{todo>ID} }'
                highlight='{= %{/detailID} === %{todo>ID} ? "Information" : "None" }'
                type='{= %{/detailID} === %{todo>ID} ? "Active" : "Navigation" }'>
                <cells>
                    <ObjectIdentifier title='{todo>name}' />
                </cells>
            </ColumnListItem>
        </items>

    </Table>

</core:FragmentDefinition>