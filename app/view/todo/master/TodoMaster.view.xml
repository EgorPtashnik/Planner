<mvc:View
    xmlns='sap.m'
    xmlns:f='sap.f'
    xmlns:mvc='sap.ui.core.mvc'
    xmlns:core='sap.ui.core'
	controllerName='planner.controller.todo.master.TodoMaster'>

    <f:DynamicPage showFooter='true'>

        <!-- HEADER -->
        <f:title>
            <f:DynamicPageTitle>

                <f:heading>
                    <Title text='Списки Дел' />
                </f:heading>

            </f:DynamicPageTitle>
        </f:title>

        <!-- CONTENT -->
        <f:content>
            <Table
                id='idTodoList'
                items='{todoService>/TodoList}'>

                <noData>
                    <IllustratedMessage />
                </noData>

                <columns>
                    <Column>
                        <Text text='Список' />
                    </Column>
                </columns>

                <items>
                    <ColumnListItem
                        press='.list.onPressTodoListItem'
                        navigated='{= %{/detailID} === %{todoService>ID} }'
                        type='{= %{/detailID} === %{todoService>ID} ? "Active" : "Navigation" }'>
                        <cells>
                            <ObjectIdentifier title='{todoService>name}' />
                        </cells>
                    </ColumnListItem>
                </items>

            </Table>
        </f:content>

        <!-- FOOTER -->
        <f:footer>
            <OverflowToolbar>
                <content>
                    <ToolbarSpacer />
                    
                    <Button
                        press='.footer.onPressCreateTodoList'
                        text='Создать Список'
                        tooltip='Создать Список'
                        type='Emphasized' />
                </content>
            </OverflowToolbar>
        </f:footer>

    </f:DynamicPage>

</mvc:View>