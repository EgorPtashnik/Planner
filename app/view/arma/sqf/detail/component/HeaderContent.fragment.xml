<core:FragmentDefinition
    xmlns='sap.m'
    xmlns:core='sap.ui.core'>

    <VBox>
        <Label text='Описание' showColon='true' />

        <!-- DISPLAY MODE -->
        <FormattedText
            htmlText='{arma>info}'
            visible='{= !%{config>/editMode} }' />

        <!-- EDIT MODE -->
        <TextArea
            width='100%'
            rows='5'
            value='{arma>info}'
            visible='{config>/editMode}' />
    </VBox>

</core:FragmentDefinition>