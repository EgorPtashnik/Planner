sap.ui.define([
    'planner/controller/BaseController'
], (BaseController) => {
    "use strict";

    return BaseController.extend('planner.todo.master.TodoList', {

        onInit() {
        }

    });
});
