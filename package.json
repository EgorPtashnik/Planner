{
  "name": "planner",
  "version": "1.0.0",
  "description": "PlanneR Application",
  "scripts": {
    "watch": "cds watch",
    "deploy": "cds deploy",
    "start": "ui5 serve -o index.html"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.2.0",
    "@sap/cds-dk": "^9.8.4",
    "@ui5/cli": "^4"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": { "url": "db.sqlite" }
      }
    }
  }
}
